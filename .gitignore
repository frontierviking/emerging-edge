__pycache__/
*.pyc
.DS_Store
.env
emerging_edge.db
emerging_edge.db-journal
emerging_edge_backup.sql
digests/
*.log
# Multi-user mode persists per-user SQLite databases under data-mu/
# (with hashed passwords + per-user Serper keys + portfolio transactions).
# Single-user fallback uses data/. Neither belongs in the public repo.
data/
data-mu/
# Generated by `python monitor.py publish` — contains a view-only HTML
# snapshot of the user's actual watchlist data, not safe to commit.
# The middleware.js / vercel.json / package.json / README.md inside are
# regenerated each run anyway.
public/
